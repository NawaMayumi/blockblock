# Codex引き継ぎプロンプト

以下を、このフォルダを開いたCodexにそのまま渡してください。

---

AHISインターンで制作している「Physical Learning Blocks」のP2を実装してください。

## 背景

子どもが緑色の立体ブロックを手で並べると、カメラが段数と列数を読み取り、デジタル画面が反応する Physical → Digital interaction platform です。掛け算はデモケースであり、将来は先生が数学・文字・図形・パターンなど別の活動を載せられる基盤にしたいです。

現在は静止画版P1が完成しています。ブラウザ内のJavaScriptだけで画像を処理し、緑色領域、個数、格子配置を判定します。白い余白を残した四角形が隣接・一部重複していても、X/Y方向の緑密度ピークから各ブロックの中心を分離します。`prototype-white-border-sample.png` は3段×4列、12個として安定検出できています。

まず既存コードとREADMEを読み、静止画機能を壊さないでください。現状のUIと検出ロジックを土台に、ライブカメラ版P2を追加してください。

## 実装してほしいこと

1. 「静止画」と「ライブカメラ」を切り替えられるようにする。
2. `navigator.mediaDevices.getUserMedia()` でカメラ映像を取得する。
3. スマホでは背面カメラを優先する。利用可能ならカメラ選択UIも付ける。
4. 動画フレームをCanvasへ取り込み、既存の緑検出処理へ渡す。処理負荷を抑えるため、解析は毎秒5〜10回程度、幅は最大1100px程度とする。
5. ライブ映像上へ、検出枠、中心点、個数、`段数 × 列数 = □` を重ねる。
6. 1フレームだけで確定しない。直近の判定履歴を保持し、同じ「個数・段数・列数」が約2秒継続したときに確定する。
7. 手が映る、一時的に隠れる、ブロックを動かしている最中は「調整中」と表示し、直前の確定結果を短時間保持する。
8. カメラ権限拒否、非対応ブラウザ、カメラ未検出、HTTPSでない場合について、日本語の分かりやすいエラーを表示する。
9. カメラ停止ボタンを用意し、停止時にはMediaStreamTrackを確実に停止する。
10. 画像や映像は外部サーバーへ送らず、すべて端末内で処理する。

## 検出上の重要条件

- 対象は「白い外周を一部残した、緑色の四角い面」を持つ同寸法の立体ブロック。
- 緑部分は印刷ではなく手描きのため、塗りむらや白抜けがある。
- ブロック同士は接触、一部重複、多少の回転があり得る。
- 背景は当面、白〜薄いグレーの無地を推奨する。
- 格子状でない配置を、無理に掛け算へ変換しない。
- X/Yピークの直積だけで存在しないブロックを補完しない。各セルの緑密度、間隔の規則性、行列の占有率からconfidenceを計算し、低信頼時は未確定にする。
- 調整用スライダーは残す。

## P2成功条件

- 実際のカメラで2×3、3×4などを置き、2秒以内程度で正しい式が確定する。
- 静止状態で10秒間、結果が頻繁に変動しない。
- ブロック移動中や手による短い遮蔽で、誤った式を即座に確定しない。
- Mac版Chromeで動作する。
- Chromebook実機入手後にChromeOSで確認すべき項目をREADMEへ列挙する。
- iPhone Safariではカメラ取得と表示を確認する。ブラウザ差がある機能はfeature detectionとフォールバックを用意する。

## 現在の環境と進め方

- 開発者はMacBook Airを使用している。
- Chromebookはまだ手元にない。
- iPhone 15 Proは使用可能。
- まずMacのChromeでライブ判定を動かす。
- カメラ映像はlocalhostまたはHTTPSでないと利用できない場合があるため、READMEへ起動方法を書く。
- 完成後、変更点、起動方法、実機テスト手順、残っているリスクを簡潔に報告する。

## 今回は後回しにするもの

- 手書き数字認識
- 教師向けカリキュラム編集機能
- ユーザー登録やクラウド保存
- ARKitなどを使った3D空間認識

実装後は構文確認だけで終わらせず、サンプル静止画で3×4＝12が維持されていることを確認してください。可能な範囲でカメラ開始・停止・履歴安定化部分もテストしてください。

---

